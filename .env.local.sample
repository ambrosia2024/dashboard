# Local overrides for `python manage.py runserver`

RUNNING_IN_DOCKER=false
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# Use console email backend locally if you do not want real emails sent.
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
